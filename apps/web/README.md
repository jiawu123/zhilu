# Web

Roadmap Workspace 前端。只通过本地 API 和共享 `contracts` 访问产品状态。

已实现路线星图、本周焦点、节点选择与编辑/拖动/完成/新增/归档、依据检查器、节点级与时间约束 Event、Impact Diff 审批、History，以及 JSON、Markdown、`.planbundle.zip` 导出。当前 Event 只确定受影响范围并记录变化原因，具体自动排期等待 Roadmapper 接入。
