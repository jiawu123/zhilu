# 当前进度 · 2026-09-13

依据：工作区已包含 M3 提交 `5c8a544`，以及合入 Kyle 新版 M2 的 `b1fd8e5`。本页的 M4、跨问题 Controller 和 M3 回放为 9 月 13 日的本地新增实现；工程测试不等于真实模型或知乎全链路验收。

## 按 PRD 模块对齐

| 模块 | 当前状态 | 主要剩余 |
| --- | --- | --- |
| M1 用户理解 | 目标/背景确认、条件化追问、背景导入已接入 | 用实际用户目标验证访谈质量 |
| M2 知乎研究 | 首轮/补充 Planner、批量证据、缓存；已接跨问题去重、全局结构覆盖和一次补检索 Controller；11:47 报告确认单问题真实 8 卡及缓存命中 | 第二个问题受配额阻塞；全局覆盖、真证据语义与用户条件覆盖、多次性能及端到端验收 |
| M3 计划编译 | 模型 Provider、路线/任务校验、确认写入；新增私有研究快照与独立离线/显式 live 回放 | 新 M2 真证据下的路线质量、真实回放与整链路验收 |
| M4 执行调整 | 图上操作、事件影响、真实字段 Diff、确认/历史；本轮新增时间变化的模型局部排期 | 其他事件、新知识缺口、任务拆分/内容调整、复盘延展及真实模型验收 |
| M5 导出 | JSON / Markdown / Plan Bundle ZIP | 公网分享属于 P1 |

## Kyle 的新增交付与剩余

旧五项清单已部分过时：新版默认 `m2-initial`、`batch-v1`，首轮共 2–3 条 Query；有显式补充规划接口；一次模型批量筛选/编译替代生产旧固定权重和逐篇编译；至多 8 卡、来源/作者限制、研究假设和 coverage；本地 24 小时 TTL 缓存已经落地。

9 月 12 日真实运行记录见 [新版交接](M2_FOLLOWUP_STATUS_2026-09-12.md)，其中“未生成新版 EvidencePack、没有真实缓存热运行”的结论已被 9 月 13 日 11:47（北京时间）的[本地实测报告](../packages/zhihu/artifacts/m2-followup-fe593e9c-5c45-4395-b4f5-7f01e288012a/report.json)部分更新：Planner 生成 2 个问题、3 条 Query，约 4.53 秒；首个问题通过 2 次真实搜索、1 次批量模型调用生成 8 张证据卡和 1 个研究候选，冷运行约 30.18 秒（搜索 3.32 秒、批量模型 26.73 秒）；随后缓存命中约 113 毫秒，搜索和模型调用均为 0。第二个问题的搜索返回 `rate_or_quota_limit`，整轮状态仍为 `failed`，总耗时约 35.83 秒；未完成跨问题全局证据验收，未运行 M3。报告使用合成用户背景、真实服务调用，证据仍需人工审阅。上述均为单次运行，不代表稳定延迟或 P95；私有 artifacts 不随 Git 文档发布。

Kyle 主要还需：补齐第二个问题及完整多问题冷/热运行；对真实来源、限制/反方观点、跨领域适用性进行人工验收；与 Jia 联合记录阶段耗时。Jia 负责的跨请求去重/总预算/结构覆盖与补检索调度已落地，M3/M4 实际模型联调仍待验收；这些不能全部算 Kyle 欠交。配额限制来自 11:47 实测报告，本次进度核对没有重新探测线上配额。

## 最新进度核对

- 当前阶段：核心实现与离线验证已就绪，M2 单问题真实证据与缓存已跑通；完整 M2 → M3 → 确认 → M4 调整仍未验收。
- 同日任务“看一下我们现在现在是什么进度”的执行记录确认 TypeScript 390 项、Python 1019 项测试通过，类型检查通过；本次仅核对记录和运行产物、重查 `git diff --check`，未重复执行测试或真实 API。
- 当前分支 `codex/p0-core-roadmap`，HEAD 为 `b1fd8e5`；相对本地记录的 `origin/codex/p0-core-roadmap` 领先 4 个提交，仍有未提交改动和新增文件。本次未 fetch、commit 或 push。
- 下一步：补齐多问题真实研究，保存可回放的研究快照，分别验收 M3 路线与 M4 时间变化排期，再完成浏览器完整 Demo 和交付收尾。

## 本轮 Controller 与 M3 回放

- Controller 首轮全局 2–3 条 Query，不足时最多补一轮 1–3 条，累计上限 6；跨轮重复、partial、配额错误停止，不自动重试或回落 Mock。
- 聚合保留 6–8 张（不足不凑数），每来源最多 2 张、每作者最多 3 张；同源同主张去重，条件差异/相反观点不合并，研究路线引用同步调整。问题、用户条件、时效一致的补搜可修复原问题空证据。
- 覆盖只检查证据数量、逐题关联、适用条件字段、限制/反例和有引用的候选；原有明确缺口保留。始终 `needs_human_review`，不宣称已完成语义覆盖判断或事实核验。
- M3 调用前将原研究准备版和投影后的研究结果保存为私有快照；模型失败不丢研究快照、不动正式 Plan/History。默认 Provider 支持总期限取消并清理子进程；注入 Provider 必须遵守取消契约。
- `apps/server/scripts/verify_m3.ts` 默认合成离线；`--snapshot ... --live` 显式只调用一次真实模型，无知乎、无自动重试、无正式写入。仅输出独立报告与预览，不将回放预览自动变为待确认提案。
- 本轮未执行真实模型或知乎调用。真实 M3/M4、OAuth 授权、公网可体验 Demo、刘看山官方素材接入和提交材料仍未在本轮完成；公网 Demo 与 P1 的个人计划只读分享不能混为一项。

配置、快照路径、回放命令和验收报告解释见 [Server 说明](../apps/server/README.md)。

## 本轮 M4 的可验证边界

- 场景仅限已确认正式路线后的每周时间变化；先保存规则预演，再由用户明确请求 AI 排期。
- 输入只包含目标、相关约束、受影响任务、父里程碑/依赖边界、必要的固定占用和最多 8 张已有证据。无原始背景全文、原文引文或历史；无新检索。
- 模型只提任务日期和理由，不能更改任务内容/工时/状态/依赖/证据，不能覆盖手工锁定。父里程碑日期由程序随子任务边界调整并校验。
- 日期、来源、范围、依赖及预算校验失败时保留旧提案。未完成工时不能分摊到过去；进行中但无剩余工时记录时按全部预计工时保守安排。固定逾期复盘需用户先确认状态或日期。
- 预计延期会明确提醒，原 Goal Contract 的目标日期不被静默修改；不自动新增或延展复盘。
- 新模型方案使用新 Patch ID；过期版本不可应用，生成期间不可批准旧提案。最终仍整组确认，逐项查看不等于逐项批准。
- 前端默认保留紧凑底栏，按需展开实际 Before → After、判断与提醒；处理记录写入 Commit 并可在 History 展开。

此前 M4 阶段的 302 个 TypeScript/跨语言边界测试全部通过，包含 M4 runtime 38 个、HTTP 12 个、前端 Diff 纯函数 5 个；新增 Controller/M3 回放后的全量验证另记于下方。这些都是离线验证，完整 Python suite 本轮未重跑。

Controller/M3 回放更新后的工作区：**390 个测试通过**（Engine 6、Runtime 118、Server 243、Web 23），`pnpm typecheck`、`pnpm build`、`git diff --check` 通过。包含同题空证据补搜、跨包去重/引用、预算/缓存/异常停止、默认 Provider 取消、HTTP 保存快照后的独立回放和前端缺口提示。CLI 默认离线回放输出 8 张合成证据、2 条结构有效预览，真实 API 调用为 0，正式写入为 0。

macOS 测试命令：`ZHIHU_TEST_PYTHON_BIN=/Users/wujia/Documents/知乎黑客松/.venv/bin/python pnpm test`。新合入的一组跨语言测试默认指向 Windows 解释器，需显式使用本机现有 Python；未为此改动 Kyle 的实现。

此前 M4 浏览器连接超时，未完成该阶段浏览器实测；本次 Controller/M3 更新只做工程与离线回放验证，未重新做浏览器实测，也未调用真实模型或知乎。后续首先用同一演示环境完成真实 M2→M3 以及时间变化→M4→确认链路。
